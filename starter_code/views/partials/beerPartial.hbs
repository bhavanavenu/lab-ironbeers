<img class="card-img-top" src="{{this.image_url}}" alt="">
  </div>
  <div class="card-body">
    <h4 class="card-title">{{this.name}}</h4>
    <p class="card-subtitle text-muted">{{this.tagline}}</p>
    <p class="card-text">{{this.description}}</p>
  </div>
</div>